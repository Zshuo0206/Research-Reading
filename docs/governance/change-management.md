# Change Management

## 变更分类

- `LOCAL`：任务范围内的私有实现，不改变契约。
- `CONTRACT`：改变公共接口、Schema、状态机、迁移或依赖，需要 RFC。
- `SCOPE`：改变 Wave 目标或新增产品能力，需要人类批准。
- `DESTRUCTIVE`：删除、覆盖、强制重置、数据迁移破坏性变更，需要人类明确批准。

## 变更记录

每个被接受的变更必须关联 Task ID、RFC/ADR（如适用）、测试结果和回滚方案。聊天中的临时决定必须转写到仓库文件，否则不视为正式决定。
