# Destructive Actions

## 默认禁止

未经人类项目负责人明确批准，不得执行：

- `git reset --hard`、强制 checkout、强制 push；
- 删除或覆盖未知文件；
- 删除 worktree、分支、数据库或对象存储数据；
- 大规模移动、重命名或格式化既有内容；
- 破坏性数据库迁移；
- 修改仓库外文件或系统配置。

## 批准前检查

必须说明目标绝对路径、影响范围、备份/回滚方案、验证方式和批准人。若命令失败，不能为了“清理”扩大破坏范围。
