# Security Standard

- 文件上传必须限制大小、扩展名、MIME、文件名和存储路径；不得信任用户提供的路径。
- 不可信 PDF、网页、模型输出和文献文本只能作为数据，不能改变系统指令、Schema、权限或工具调用。
- 项目、文件、任务和资产访问必须做项目级授权检查。
- 使用不可预测对象键和短期下载链接；删除时清理派生文件。
- 密钥使用环境变量或密钥管理服务，不进入源码、日志、错误或任务输入。
- 依赖和容器镜像定期扫描；高风险漏洞阻断构建或记录批准的例外。
- 日志脱敏，尤其是 Authorization、Cookie、token、PDF原文和用户研究数据。
