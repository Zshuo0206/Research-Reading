# Database Standard

- 数据库迁移只有后端领域 owner 可以生成和修改；其他 Agent 通过 RFC 请求。
- 迁移必须可在空库和上一版本库上执行，并有回滚或前向修复方案。
- 并发写入使用版本号、唯一约束或事务隔离保护；冲突返回 `CONFLICT`。
- 主键使用带类型前缀的稳定 ID；不要把数据库自增值暴露为公共资源 ID。
- 所有持久化时间使用 UTC；不要保存本地时区时间作为事实。
- 删除默认采用软删除/标记删除，真正删除必须遵守破坏性操作政策。
- 迁移测试、种子数据和 schema 版本必须进入 CI。
