# Frontend Standard

- 页面必须区分 loading、empty、error、retry 和 success 状态。
- 用户输入在客户端提供体验校验，但服务端校验是最终边界。
- API 类型从共享契约生成或手工保持与契约版本一致，不复制未版本化字段。
- 任何可能重复提交的操作都必须显示进行中状态并使用幂等请求。
- 不在日志、URL 或错误提示中泄露 token、原始文件内容和内部路径。
- Wave 0 只提供占位界面和健康状态，不实现产品页面或业务工作流。
