# Configuration Standard

- 配置来源优先级必须固定：进程环境 > 明确配置文件 > 安全默认值。
- 仓库只提交 `.env.example`，不提交 `.env`、证书、token 或真实 endpoint 密钥。
- 启动时校验必需配置；缺失配置要快速失败并给出安全错误。
- 配置值不得在日志中完整打印；URL 中的凭据必须脱敏。
- 不同环境使用显式 profile；禁止根据字符串猜测生产环境。
- 配置键、默认值、敏感级别和 owner 必须有文档。
