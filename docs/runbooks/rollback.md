# Rollback

## 代码回滚

优先使用可审计的反向提交或修复提交；禁止未经批准的强制 reset。记录受影响任务、提交、验证和后续恢复计划。

## 数据/配置回滚

必须先确认备份、迁移兼容性、停止写入窗口和回滚验证。破坏性数据操作需人类项目负责人明确批准。

## Agent 回滚

若 Agent 输出不可信，隔离其分支/worktree，不把草稿资产写入正式库；保留日志和失败证据，按失败 Agent runbook 重新分配。
