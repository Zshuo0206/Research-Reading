# Failed Agent Recovery

1. 将任务标记为 `PARTIAL` 或 `BLOCKED`，保留交接和日志。
2. 记录失败命令、最后已知工作、未提交修改和依赖。
3. 主控 Agent 检查 diff，禁止用 reset 覆盖未知修改。
4. 若失败是环境问题，先修复环境并复跑；若是范围问题，提交 RFC/拆分任务。
5. 可继续的任务优先复用原 Agent；不可继续时由主控重新分配新 Agent，但必须交接已完成内容。
6. 重新运行关键测试并更新状态，确认恢复后才可集成。
