# Development Log — 2026-07-11

## Wave and objective

Wave 0：建立工程治理、最小无业务骨架和多 Agent 干运行流程。

## Completed

- 完成空工作区审计并记录 KEEP/REFACTOR/MIGRATE/DELETE/UNKNOWN 分类。
- 初始化 Git 仓库并建立 `main` 基线。
- 建立 AGENTS、治理、任务模板、编码规范、质量门槛和恢复 runbook。
- 建立前端/API/worker 占位、共享 Schema、统一 Node 检查、Compose 和 CI。
- 创建两个独立分支和 worktree，启动两个 Codex 子 Agent 并完成单文件交接。
- 模拟公共接口变更并通过 RFC 拒绝其在 Wave 0 实现。
- 模拟测试不可执行限制并按失败 Agent runbook 恢复。

## In progress

- Wave 0 结果等待人类项目负责人验收。

## Decisions / ADRs / RFCs

- `RFC-DRY-001`：拒绝在 Wave 0 增加健康响应字段。
- 生产技术栈、数据库和真实依赖留待 Wave 1 ADR。

## Interface changes

- 无实际公共接口变更。
- 仅保留 `packages/contracts/wave0.schema.json` 占位契约。

## Test status

- 主控 Smoke/结构检查：通过，`node scripts/check.mjs all`。
- 子 Agent T-DRY-A/T-DRY-B：文档任务，主控已复核文件边界；无业务测试适用。
- 真实单元、集成、契约、E2E、AI、数据库迁移和完整安全扫描：按范围未适用/待后续技术栈。

## Blockers

- 等待人工验收，不能自动进入 Wave 1。

## Risks

- 目前命令入口为无依赖占位检查，不等同于生产 lint/typecheck/security 工具链。
- worktree 清理必须在主控确认分支交付后完成。

## Next actions

- 主控完成最终一致性审查。
- 集成两个 dry-run 审计文件。
- 清理完成的 dry-run worktree。
- 更新 CURRENT_STATE 后停止等待人工验收。

## Final Wave 0 gate

- 主控复跑 `node scripts/check.mjs all`：通过。
- `main` 工作区：无未提交修改。
- 两个 dry-run 分支：已按集成顺序合并；临时 worktree 已清理，分支保留用于审计追踪。
- 人工验收：等待中；不自动进入 Wave 1。

## Governance correction — human acceptance boundary

- 将技术验收责任明确归属责任 Agent、QA Agent 和主控 Agent。
- 明确主控 Agent 对技术验收结论和证据完整性负责。
- 明确人类项目负责人只审核产品目标、范围偏移和重大事项，不逐项审核工程细节。
- 新增 `docs/templates/human-acceptance-summary.md`，作为每个 Wave 的统一产品验收输入。
- 新增当前 Wave 0 摘要 `docs/audits/wave0-human-acceptance-summary.md` 和责任边界审查 `docs/audits/governance-boundary-review.md`。
- 本次只修改治理文档和状态记录，不重新执行 Wave 0，不进入 Wave 1。
- 当前 Wave 0 摘要已校正 QA 角色记录：本轮未单独配置 QA Agent，主控复核不替代独立 QA 证据。

## Human acceptance

- 人类项目负责人结论：Wave 0 通过。
- 验收依据：内部技术验收通过；无已记录的失败或被绕过的强制检查；产品目标和阶段范围符合预期；未提前进入真实业务开发；人工验收责任边界已调整清楚。
- 未配置独立 QA Agent 不构成 Wave 0 阻塞；Wave 1 真实业务实现前必须明确 QA 的独立执行方式。
- 当前状态：保持暂停，不创建 Wave 1 真实业务实现任务，不开始 Wave 1 编码。

## Wave 1 product scope review

- 人类项目负责人结论：有条件批准，需要调整后再次提交最终范围，不授权立即编码。
- 产品范围已调整为个人单用户 Web 工作台、方法学习、真实可提取文本 PDF、外部 ModelGateway + Mock，以及问题—证据回答—用户确认闭环。
- 已补充主要产品指标：问题可用率、带证据回答的用户接受率；并记录问题修改率、回答拒绝原因、完成时间和相对普通摘要的帮助程度。
- 已明确区分已决定事项、技术论证事项和仍需人类决定事项。
- 未创建 Wave 1 任务、分支或 worktree，未开始编码。

## Wave 1 human product approval and technical kickoff

- 人类项目负责人正式批准 Wave 1 产品范围：个人单用户 Web 工作台、方法学习、真实可提取文本 PDF、外部 ModelGateway + Mock，以及问题—证据回答—用户确认闭环。
- 批准的边界包括：回答区分 `PAPER_FACT`、`AUTHOR_CLAIM`、`AGENT_INFERENCE`；未经确认的内容不作为正式研究资产；不纳入 OCR、扫描 PDF、复杂表格/公式理解、多篇综合、正式资产库、导出、协作和插件。
- Wave 1 状态更新为“产品范围已获人工批准，等待技术方案冻结”；本轮未开始真实业务编码。
- 主控完成技术方案提案：Node 24 LTS/TypeScript（升级 Wave 0 Node 22 基线）、Fastify、React/Vite、SQLite、内容寻址文件存储、pdfjs 按页提取、JSON Schema/Ajv、独立 Worker、ModelGateway + Mock、独立 QA Agent 和固定评测集。
- 新增 `docs/architecture/wave1-technical-plan.md`，记录技术栈、PDF 处理、数据存储、模型发送边界、QA、评测、CI、风险和回滚。
- 新增 `docs/rfcs/RFC-W1-001-technical-foundation-and-contract-baseline.md`，状态为 `PROPOSED`；新增 T-W1-001 至 T-W1-008 规划任务，全部保持 `DRAFT`，不授权分配或编码。
- 更新 Wave 1 ownership map，明确契约、平台、PDF/存储、ModelGateway、工作流、前端、QA 和主控职责；同一文件只允许一个 owner 写入。
- 技术启动结论：已具备进入技术冻结检查的条件；尚不具备真实业务编码授权，完成冻结后需再次申请编码启动。

## Wave 1 technical review remediation

- 根据技术审核意见修订 Wave 1 启动材料；未开始真实业务编码，未创建实现分支或 worktree，未安装真实依赖。
- 将启动流程拆为三个独立 Gate：Gate A 技术方案冻结、Gate B 人类编码授权、Gate C 主控逐任务解锁；当前分别为 `PENDING_REVIEW`、`NOT_GRANTED`、`LOCKED`。
- 统一 Node.js 24 LTS 为目标基线，并明确“Node 24作为更新的LTS目标基线；Node 22仍处于LTS支持期”。
- 冻结 Wave 1 仅本地 `127.0.0.1` 部署；认证主体为本地操作员，不再使用没有认证主体的“项目级授权”表述。
- 重构 Question/Answer 为 revision + `review_status` + `verification_status`；`MODIFIED` 不再是稳定状态。补充 Job 正式迁移表、SQLite WAL/claim/lease/recovery/checkpoint、PDF 资源限制和临时文件清理规则。
- 完整定义 `canonical_page_text`、Unicode code point offset、`[start,end)`、quote、UTF-8 SHA-256 和 `pdfjs-text-v1`；ModelGateway 改为模型返回 `context_span_id`/候选引用，由服务端物化最终 EvidenceSpan。
- 为 `question-plan.v1` 增加 `document_language`、`retrieval_queries` 和 `retrieval_terms`，处理中文问题与英文论文的词法检索。
- 明确 Wave 1 验收只要求 ModelGateway + deterministic Mock；真实外部供应商适配器移出 Wave 1，后续需要单独供应商 RFC。
- 指定独立 QA owner 为 `qa-evaluation-agent`，并在 RFC-W1-001 增加 reviewer sign-off 表；尚无 reviewer 已签字，RFC 保持 `PROPOSED`。
- 拆分原 T-W1-003 为 T-W1-003A 存储/Job runtime 与 T-W1-003B PDF/Evidence；新增 T-W1-006A 契约 Web 骨架，使 Mock、评测资料和 Web skeleton 可更早并行。
- 将 PR Playwright smoke 纳入确定性 CI 门槛；问题可用率和带证据回答接受率保留为产品观察指标，不作为确定性 CI 阻断条件。
- 当前停止等待 Gate A 冻结复审；所有实现任务继续 `DRAFT`，编码授权继续 `NOT_GRANTED`。

## Wave 1 first-batch coding and acceptance

- 本提示词作为人类项目负责人决定记录：Wave 1 必须包含 deterministic Mock、统一 OpenAI-compatible BYOK 适配器、OpenAI/Gemini/Groq/OpenRouter 预设、自定义 HTTPS `base_url`/模型、连接测试和真实模型人工验收；不提供平台共享密钥，API Key 不进入 Git/SQLite/日志/审计/导出/fixture。
- 完成规划一致性收口：`wave1-technical-plan.md`、启动摘要、`RFC-W1-001`、任务、ownership map、CURRENT_STATE 均已同步；Gate A=`PASSED`，Gate B=`GRANTED`（仅第一批），Gate C 仅解锁 T-W1-001/T-W1-002/T-W1-007。
- T-W1-001 完成 `wave1.v1`、`api.v1`、`document.v1`、`job.v1`、`question-plan.v1`、`answer.v1`、`evidence.v1`、`model-gateway.v1`、统一错误、revision/status、Job 迁移、canonical Evidence、BYOK 配置和 Schema/TypeScript 校验流程。
- T-W1-002 完成 Node 24 (`v24.16.0`) 迁移目标、真实 npm workspace 依赖和 lockfile、Biome、TypeScript strict、Vitest、Ajv、构建、CI、secret/audit/license 检查，并保留 loopback 占位 API/Worker 启动入口。
- T-W1-007 完成 metadata-only 评测 manifest、来源/许可/hash 规则、人工参考格式、拒答格式、Mock fixture 和 QA 清单；因未提交无许可论文 PDF，任务标记 `PARTIAL`，未伪造真实评测或 QA 签字。
- 实际通过：`npm ci --ignore-scripts`、`npm run format`、`npm run lint`、`npm run typecheck`、`npm run test`、`npm run contract`、`npm run build`、`npm run security`、`npm run ci`、`node scripts/check.mjs all`、`git diff --check`。
- 明确未实现：SQLite 业务表、PDF 解析、文件内容寻址、MockModelGateway 运行时、真实 BYOK HTTP、项目/API、Web 业务页面、Playwright 完整闭环、Markdown/Excel 导出；完成后停止，不自动进入下一批。

## External review handoff packaging

- 已将外部审核交接流程固化到 `docs/process/external-review-handoff.md`，并在 `AGENTS.md` 增加最小引用。
- 现有第一批 25 个审核文件未重新生成；复用原 review-bundle，仅补充 Node/npm 版本和附加安全检查日志。
- 已整理为 `external-review-summary.md` 和 `wave1-first-batch-external-review.zip`；未修改业务实现、任务状态或 Gate 状态，未启动下一批。

## Wave 1 first-batch repair

- 在保留原有 dirty workspace 的前提下创建一次性分支 `repair/wave1-first-batch`；未创建额外 worktree、未 reset/clean/stash、未删除旧审核材料。
- 修复公共契约阻塞：API success/error envelope 互斥、统一 error object、ContextSpan/EvidenceSpan 分离、Answer 使用 evidence span 引用、ModelGateway 三类 discriminated operation、Schema 生成/漂移检查和 BYOK secret 分离。
- 修复平台阻塞：Node 24 workspace strict typecheck、Biome 真实格式检查、contract workspace include、API/Worker/Web loopback shell、platform smoke、Playwright platform smoke、security/license/audit 检查和 CI 浏览器安装入口。
- 实际复跑契约、格式、lint、类型、build、unit/contract、platform smoke、Playwright platform smoke、security、规划检查；本机 Chromium 下载超时，平台 E2E 使用已安装 Microsoft Edge channel 成功执行。失败/超时不伪装为成功。
- T-W1-001/T-W1-002 状态调整为 `REVIEW`，T-W1-007 保持 `PARTIAL`；Gate A 标记为 `REPAIR_REVIEW_PENDING`，Gate B 保持人类已记录的第一批授权，Gate C 保持 `LOCKED`。未进入 SQLite、PDF、Gateway 运行时、业务 API 或 Web 业务开发。
- 独立复核尚未完成；不在本日志中给出最终审核结论或下一批授权。
