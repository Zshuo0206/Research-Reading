# Development Log — 2026-07-12

## Wave 1 first-batch final repair

- Scope remained T-W1-001/T-W1-002/T-W1-007 only. T-W1-001 and T-W1-002 remain `REVIEW`; T-W1-007 remains `PARTIAL`; Gate C remains `LOCKED`.
- ModelGateway was changed from a combined execution record to complete `REQUEST/RESPONSE + operation` variants. Question and answer responses are draft-only and contain no persistent IDs, revisions, timestamps, hashes or review state.
- BYOK requests have one top-level invocation-only `runtime_secret_ref`; Mock requests omit it. Request/response exclusivity and answer/connection conditional constraints have positive and negative tests.
- The Schema inliner now switches context when following external `$ref`. Generated TypeScript has concrete question draft fields and compiles discriminator narrowing tests. A temporary Schema mutation proves generated drift is detected.
- Added `validateContextSpan` and `validateEvidenceSpan` semantic checks for right-open Unicode intervals, canonical quote/text, page bounds, page hash, document version and extraction profile identity.
- Removed duplicate `.mjs` API/Worker shells, stale Node 22 per-app Dockerfiles and the obsolete second `docker-compose.yml`. `scripts/check.mjs` now builds and starts the formal TypeScript/Fastify API shell; `compose.yaml` is the sole Compose definition.
- Local API/Web host policy rejects non-loopback hosts; explicit container mode permits internal `0.0.0.0`, while Compose host publishing remains bound to `127.0.0.1`.
- Security scanning now uses complete file enumeration for `.env`, PDF, SQLite/database and private-key files, and text-only scanning for secret patterns. Four temporary forbidden-file probes are created, rejected and cleaned automatically.
- T-W1-002 and ownership paths now assign platform shell files to the platform owner and reserve specific future Web business directories for the frontend owner.
- Removed the tracked repair summary and deleted the stale ignored repair ZIP. This round uses `implementation_base=b750a25f1bac7ee7934122651819c4805fa694e5` and will report only the final implementation range.

## Verification evidence

- `npm ci`: exit 0; Node `v24.16.0`; 180 packages audited; 0 vulnerabilities.
- format, lint, typecheck, test, contract, build, smoke, Playwright smoke, security, unified CI, planning/structural check and `git diff --check`: exit 0 in the final rerun.
- Vitest: 2 files, 11 tests, no skipped/todo/disabled tests.
- Playwright: 1 platform smoke test, using installed Microsoft Edge on Windows.
- Contract: 9 Schemas, deterministic generation, drift probe, request/response conditions and 4 metadata-only evaluation samples.
- Security: 4 temporary forbidden-file probes rejected and cleaned; npm audit 0 vulnerabilities.
- `docker compose config`: exit 1 because `docker` is not installed or discoverable on this host. Compose up/build/health/down was therefore not executed.

## Scope guard

No PDF parser, SQLite runtime/table, ModelGateway HTTP runtime, real external model call, business API, business UI or export implementation was added. No merge to `main` was performed and no final independent review conclusion is recorded here.

## Wave 1 first-batch final directed repair

- This repair continues from `97aa1345fd6c42b69b9b60b574ab2e2b5a8c7f4f`; task states remain T-W1-001 `REVIEW`, T-W1-002 `REVIEW`, T-W1-007 `PARTIAL`, and Gate C `LOCKED`.
- `runtime_secret_ref` is now a strict invocation-only union: `ENVIRONMENT { name }` or `SESSION_MEMORY { handle: secret_session_* }`. Real BYOK variants require it and Mock variants omit it.
- Answer generation now receives a complete confirmed-question snapshot (`question_id`, `revision_id`, `text`). Question-plan requests require at least one context span; QuestionPlanDraft shares the formal question-plan language, retrieval and question-text limits.
- Connection test responses use the unified provider enum including `MOCK`; generated discriminator types cover the strict secret variants without handwritten duplicate interfaces.
- Security scanning rejects every non-template `.env*` file, databases and private keys. PDFs are rejected by default; only a registered SHA-256-matched, source- and license-recorded, redistributable fixture under `tests/fixtures/pdf/` is eligible. The repository manifest contains no PDF fixture.
- CI now runs `docker compose config` immediately after `npm ci`. Docker remains unavailable on this local host, so no Compose runtime was started.
- The platform owner owns the PDF fixture manifest used by security policy; the later PDF/evidence owner retains only PDF fixture content paths. No real PDF was added.
