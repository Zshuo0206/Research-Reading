# Development Log — 2026-07-16

## Wave and objective

T-W1-013 已进入 `main` 基线 `7f7684d8ed6107b10bb3d8091d45984a240feb90`。
本轮在隔离分支实现并将 T-W1-014 合并进入 `main`，将 Guided Learning 生成接入现有 SQLite Job/Worker runtime。
本轮不代表整个 Wave 1 或完整产品完成。

## Completed

- 新增 `GUIDED_LEARNING_DIRECTION_GENERATION`、`GUIDED_LEARNING_QUESTION_GENERATION`、
  `GUIDED_LEARNING_FEEDBACK_GENERATION` 和 `GUIDED_LEARNING_STAGE_SUMMARY_GENERATION`。
- Session 创建、命令状态更新和 Guided Learning Job 入队使用同一个 SQLite transaction。
- Worker 校验 schema version、operation、Session、revision 和状态；旧 revision 不能覆盖新 Session。
- Worker 使用现有 ModelGateway/Mock；点评 Evidence 从 READY document version 的 canonical page text
  物化，保留真实 page number、code-point 坐标、quote、page hash 和 extraction profile。
- 模型失败记录 Guided Learning retryable failure，RETRY 可恢复到服务端 resume state；重复命令和已完成生成保持幂等。
- 新增连续 migration `005_guided_learning_worker_jobs.sql`，不修改已发布 migration。

## Test status

- Guided Learning runtime + Worker 定向测试：11/11 通过。
- 覆盖四类 handler、真实 `synthetic-text.pdf` 闭环、Evidence 回查、失败重试、非法 payload、缺失 Session、旧 revision 和命令幂等。
- 未执行真实 BYOK 人工验收，不宣称模型联网路径完成；下一步是 T-W1-015 Guided Learning Web Workbench；ANALYZE、TRANSFER 和完整 Wave Gate 仍未完成。

## Files and boundaries

本轮未修改公共契约 Schema、generated types、Web、PDF fixture 或 planning 脚本。
快速问答 Job/runtime 保持原有路径并继续回归验证。
