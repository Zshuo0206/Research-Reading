# Development Log — 2026-07-16

## Wave and objective

T-W1-013 已进入 `main` 基线 `7f7684d8ed6107b10bb3d8091d45984a240feb90`。
本轮在隔离分支实现并将 T-W1-014 合并进入 `main`，将 Guided Learning 生成接入现有 SQLite Job/Worker runtime。
本轮不代表整个 Wave 1 或完整产品完成。

## Completed

- 新增 `GUIDED_LEARNING_DIRECTION_GENERATION`、`GUIDED_LEARNING_QUESTION_GENERATION`、
  `GUIDED_LEARNING_FEEDBACK_GENERATION` 和 `GUIDED_LEARNING_STAGE_SUMMARY_GENERATION`。
- Session 创建、命令状态更新和 Guided Learning Job 入队使用同一个 SQLite transaction。
- Worker 校验 schema version、operation、Session、revision 和状态；旧 revision 不能覆盖新 Session。
- Worker 使用现有 ModelGateway/Mock；点评 Evidence 从 READY document version 的 canonical page text
  物化，保留真实 page number、code-point 坐标、quote、page hash 和 extraction profile。
- 模型失败记录 Guided Learning retryable failure，RETRY 可恢复到服务端 resume state；重复命令和已完成生成保持幂等。
- 新增连续 migration `005_guided_learning_worker_jobs.sql`，不修改已发布 migration。

## Test status

- Guided Learning runtime + Worker 定向测试：11/11 通过。
- 覆盖四类 handler、真实 `synthetic-text.pdf` 闭环、Evidence 回查、失败重试、非法 payload、缺失 Session、旧 revision 和命令幂等。
- 未执行真实 BYOK 人工验收，不宣称模型联网路径完成；下一步是 T-W1-015 Guided Learning Web Workbench；ANALYZE、TRANSFER 和完整 Wave Gate 仍未完成。

## Files and boundaries

本轮未修改公共契约 Schema、generated types、Web、PDF fixture 或 planning 脚本。
快速问答 Job/runtime 保持原有路径并继续回归验证。

## T-W1-015 Guided Learning Web Workbench

- 在独立分支新增 Guided Learning 入口，复用 `api.v1` envelope 和 `guided-learning.v1` Session/Command，完成项目创建、真实文本 PDF 上传、学习目标、方向选择、UNDERSTAND 路线、逐题回答/跳过、点评、参考答案、Evidence 和阶段总结。
- 新增统一 Session/Job 轮询：请求完成后再安排下一次，支持 Abort、有限网络重试、失败安全摘要和 `RETRY`；Session ID 写入 `?guidedSession=`，刷新后重新读取服务端状态。
- Evidence 只展示服务端返回的页码、quote、字符位置、profile、缩短哈希和验证状态；ANALYZE、TRANSFER 保持锁定。
- 为 `EDIT_ANSWER` 补充反馈 Job 重入队和旧反馈清理，未修改公共契约或数据库 migration。
- Web API client/轮询测试 5/5；Guided Learning runtime + Worker 定向测试 12/12；Playwright Web smoke 3/3，包含快速问答回归、真实 `synthetic-text.pdf` fixture 上传和 Mock-shaped Guided Learning 失败重试、Evidence、阶段总结、刷新恢复流程。
- 已在 `main` 合并提交 `42fc805cd5759e15b769fcbd03c96d31c9833772` 完成 T-W1-015 收口；Web API client/轮询 5/5、Guided Learning runtime + Worker 12/12、runtime integration 26/26、`npm test` 24/24、Playwright smoke 3/3、contract、build、lint、typecheck 和 security scan 通过。
- 未执行真实 BYOK 人工验收；不宣称联网模型路径、ANALYZE、TRANSFER 或整个 Wave 1 Gate 完成。下一步是使用真实可提取文本 PDF 完成 Wave 1 完整 E2E，再进行 BYOK 人工验收和产品验收。

## T-W1-016 Wave 1 Guided Learning 验收与发布收口

- 在独立 `chore/w1-acceptance-release` worktree、基线 `b8e60fc2cd2f0942243b01ae59cce2d962ef56ee` 上完成 T-W1-016 自动验收。
- 全新 SQLite migration 001–005、schema version 5、既有/Guided Learning Job kind、外键/索引、API/Worker 同库和重启恢复通过；登记的 `synthetic-text.pdf` SHA-256 保持 `99e07b4ba995b7c90bd84628a5db55b71a4faa1f06d3714a5942741cd39a55f8`。
- 完整门禁通过：planning、check all、lint、typecheck、runtime integration 26/26、`npm test` 24/24、contract、build、Playwright E2E 3/3、smoke、offline security scan、`git diff --check`；Guided Learning client/轮询 5/5、runtime/Worker 12/12、BYOK/Mock 17/17。
- 验收发现并最小修复跨 workspace `RuntimeSecretError` 分类问题，修复提交为 `5443766`；修复后完整验证重新通过。
- Mock 技术验收通过，但发布判定为 `RELEASE_CANDIDATE_WITH_OPEN_BLOCKERS`。固定 Mock provider、API/Worker secret 不共享、内容仍为固定模板、Evidence 尚未语义 grounding、PDF 刷新/页码跳转缺失；真实 BYOK 人工验收因缺少用户凭据标记 `BLOCKED_BY_MISSING_USER_CREDENTIALS`。未执行 online `npm audit`，不宣称真实模型或完整产品验收完成。
