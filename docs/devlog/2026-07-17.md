# Development Log — 2026-07-17

## T-W1-017 Real BYOK and Evidence Grounding

- 从已推送 main `3df69784de5c3ebbccae36790f2a1e63a8e92ed3` 创建 `feat/w1-real-byok-evidence-grounding`。
- 新增 migration 006 provider config；实现四类 Guided Generation operation、服务端 provider 白名单、Worker `WORKFLOW_BYOK_API_KEY` 和 canonical PDF Evidence grounding。
- 新增 PDF content endpoint、刷新恢复、Evidence 页码按钮、BYOK 配置界面和服务端环境连接测试。
- Review repair：为四类 operation 增加专用 input builder 和 operation-specific output requirements；Summary 增加 `major_weak_points`，Feedback/Summary 对 malformed output fail closed；selected direction 由 Session 服务端对象解析。
- Evidence review repair：只执行 canonical source verification，使用唯一 code-point quote、document/context/claim type 校验和 Evidence key 去重，不执行 claim/quote 语义蕴含判断。
- 自动化：BYOK/Evidence 3/3、PDF/BYOK HTTP 2/2、runtime 26/26、npm test 24/24、Playwright 3/3（BYOK 控件 + Mock UI，真实 BYOK 浏览器 E2E 未执行）、contract/typecheck/lint 通过。
- 真实 BYOK 因没有用户凭据保持 `BLOCKED_BY_MISSING_USER_CREDENTIALS`；不把 fake provider 结果写成真实发布通过。

## T-W1-018 PDF upload body-limit repair

- 人工验收使用 8.34 MiB（8,745,861 bytes）真实文本 PDF 时，浏览器 POST 出现 `ERR_CONNECTION_ABORTED`，而 health 正常；定向代码/测试确认根因为 Fastify 默认 1 MiB bodyLimit 在 PDF 内容解析阶段拒绝请求，该问题属于 V1.0 发布阻断。
- PDF 上传路由改为显式 32 MiB route-specific bodyLimit（`PDF_UPLOAD_MAX_BYTES = 32 * 1024 * 1024`），普通 JSON API 未扩大；超限请求返回保留 CORS 的 api.v1 `413/PDF_TOO_LARGE`，不创建 document、document version 或 Job。
- 动态 2 MiB、约 9 MiB、超过 32 MiB、普通 JSON bodyLimit、CORS 和 Web client 可读错误定向测试 10/10 通过。真实 8.34 MiB PDF 的浏览器重新上传仍待用户人工复验；`REAL_BYOK_ACCEPTANCE` 不标记 `PASS`，不标记 `READY_FOR_V1_0_TAG`，不创建 tag。
- 修复后的完整回归通过：backend packages、planning 19 tasks、`check.mjs all`、lint、typecheck、runtime integration 26/26、`npm test` 24/24、contract、build、e2e smoke 3/3、platform smoke、security scan `--skip-audit` 和 `git diff --check`；隔离临时正式 API 验证 9 MiB 为 HTTP 200，超限为 HTTP 413/PDF_TOO_LARGE 且 CORS 正确。

## T-W1-018 V1.0 Release Gate

- T-W1-017 已通过 no-ff merge 进入 main，merge commit 为 `54043fb28cb082eb871773732bafce0f031fc562`。
- Release Gate 自动化复核通过：合并前 main planning 18 tasks；加入 T-W1-018 后当前 planning 19 tasks，runtime 26/26、BYOK/Evidence 13/13、`npm test` 24/24、Playwright 3/3、contract 无 drift、lint/typecheck/build/smoke/security/diff-check 通过。
- PDF fixture SHA-256 保持 `99e07b4ba995b7c90bd84628a5db55b71a4faa1f06d3714a5942741cd39a55f8`；未运行全仓 format。
- 当前环境未发现 `WORKFLOW_BYOK_API_KEY`，因此真实外部 BYOK、真实浏览器 BYOK 和 Evidence 人工抽查未执行；V1.0 保持 `BLOCKED_BY_MISSING_USER_CREDENTIALS`，不创建 tag。

## T-W1-018 Worker polling repair

- 人工 Gate 发现正式 `npm run worker` 只输出 ready 后退出，根因是入口没有调用 `JobRuntime.runOnce()` 或持久轮询；该问题属于 V1.0 发布阻断。
- 新增可测试 Worker loop/service：顺序消费 Job、无 Job 默认 idle 250ms、stop 后不领取新 Job、等待当前 Job 完成、数据库只 close 一次、fatal error 安全摘要、smoke 立即退出。
- 定向 Worker loop 测试 9/9 通过，包含真实 SQLite Job 入队、正式 `JobRuntime.runOnce()` 消费并成功完成；构建后 Worker CLI ready 后保持运行，smoke 子进程正常退出。当前非交互 runner 无法注入真实键盘 Ctrl+C，等价 graceful-stop 已覆盖，需用户终端补做 Ctrl+C 确认。
- 修复不改变真实 BYOK 验收状态：仍为 `BLOCKED_BY_MISSING_USER_CREDENTIALS`，不得标记 `READY_FOR_V1_0_TAG`。

## T-W1-018 DeepSeek V4 Guided Feedback JSON repair

- 第三个真实阻断：DeepSeek connection test、方向生成和 6 个问题生成通过；第一题提交后 `GENERATE_FEEDBACK` 连续两次失败，页面为 `RETRYABLE_FAILURE`，错误是 provider message content 非合法 JSON，revision 7 重试到 revision 9。旧 Worker 没有逐任务响应诊断，不能从历史记录确定是 `finish_reason=length`、空 content 或 malformed JSON。
- 调查确认网关原请求有 JSON Output 和 `max_tokens=2000`，但没有 DeepSeek V4 的 `thinking: { type: "disabled" }`，且 JSON prompt 没有完整 operation 示例；官方文档说明 thinking 默认开启、JSON Output 需要 JSON 字样和示例、`finish_reason=length` 可能截断且 content 可能为空。
- 修复仅对 `CUSTOM_OPENAI_COMPATIBLE` + `api.deepseek.com` 的结构化请求关闭 thinking；保留 `response_format`，四个 Guided operation 增加完整 JSON example 和禁止 fence/额外文本；解析增加 trim/BOM 最小规范化和安全诊断，finish length、empty/null、schema invalid、malformed JSON 均 fail closed。非 DeepSeek provider 不携带专有字段；Worker 默认诊断日志只输出 status、finish reason、长度、provider/model/operation。
- 定向 BYOK/Guided Learning 17/17 通过；真实 API 不在本轮自动调用，真实浏览器重试待用户执行。`npm run ci` 首步 format 因仓库既有 17 个格式差异失败，未执行全仓格式化；后续 lint、typecheck、runtime 26/26、npm test 24/24、contract、build、smoke、e2e 3/3、security 和 diff-check 均通过。API/Worker 重启后原 RETRYABLE_FAILURE Session 可直接点击 RETRY，不需重建项目、PDF 或 Session；不改变 `REAL_BYOK_ACCEPTANCE`、`READY_FOR_V1_0_TAG` 或 tag 状态。

## T-W1-018 第四个真实阻断：RETRY feedback question pointer

- 真实验收 Session `learning_95664836-e2bb-4c74-9e07-496448c0e3e1` 在第一题反馈失败后变为 `FAILED`、revision 11，浏览器错误为 `Feedback question pointer changed`。这是 V1.0 发布阻断；无需用户重新建项目、上传 PDF、选方向或回答问题。
- 根因是 API 的 RETRY 分支在恢复 `GENERATE_FEEDBACK` 时没有把当前题目传给 `generationJob`，Job 因而缺少 `question_id`/`question_order`；Worker 直到模型调用后才检测题目指针，`GuidedLearningWorkerError` 默认不可重试，导致 Session 永久失败。
- 修复按 operation/job kind 明确区分题目指针：SUBMIT_ANSWER、EDIT_ANSWER 和反馈 RETRY 必须生成合法当前题目指针；方向、问题和阶段总结 RETRY 不携带指针；API 创建 Job 和 Worker parse/入口均 fail closed，Worker 在 ModelGateway 前拒绝缺失或不匹配指针。
- 新增 `tests/integration/guided-learning/retry-pointer.test.ts`，覆盖反馈任务指针、三类非反馈 RETRY、缺失指针不调用 ModelGateway、失败→RETRY→FEEDBACK_READY、EDIT_ANSWER 和幂等重放；DeepSeek JSON 兼容性回归测试保持纳入定向验证。
- 新增 `scripts/repair-wave1-manual-session.mjs`。脚本固定 allowlist 到 `D:\Research-Reading-Acceptance\wave1-manual\run.sqlite` 和该 Session，严格校验 FAILED/revision 11、UNDERSTAND、第一题答案和指定 failure 字段后，在单事务中恢复为 `RETRYABLE_FAILURE`/revision 12，并将 active failure 设为 retryable；不修改项目、PDF、答案、问题、证据或 provider config，重复运行安全退出。
- 自动化修复后真实浏览器复验仍待用户执行：刷新页面并直接点击“重试生成”。本次不调用真实 DeepSeek，不标记 `REAL_BYOK_ACCEPTANCE = PASS`，不标记 `READY_FOR_V1_0_TAG`，不创建 tag。
- 本轮复核通过：题目指针定向测试 5 个文件 32/32、runtime integration 26/26、`npm test` 24/24、Playwright 3/3、planning/check、lint、typecheck、contract、build、smoke、安全扫描和 `git diff --check` 全部通过；恢复脚本第二次执行幂等退出。真实浏览器刷新并点击 RETRY 仍待用户执行。
