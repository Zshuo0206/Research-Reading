# Development Log — 2026-07-17

## T-W1-017 Real BYOK and Evidence Grounding

- 从已推送 main `3df69784de5c3ebbccae36790f2a1e63a8e92ed3` 创建 `feat/w1-real-byok-evidence-grounding`。
- 新增 migration 006 provider config；实现四类 Guided Generation operation、服务端 provider 白名单、Worker `WORKFLOW_BYOK_API_KEY` 和 canonical PDF Evidence grounding。
- 新增 PDF content endpoint、刷新恢复、Evidence 页码按钮、BYOK 配置界面和服务端环境连接测试。
- Review repair：为四类 operation 增加专用 input builder 和 operation-specific output requirements；Summary 增加 `major_weak_points`，Feedback/Summary 对 malformed output fail closed；selected direction 由 Session 服务端对象解析。
- Evidence review repair：只执行 canonical source verification，使用唯一 code-point quote、document/context/claim type 校验和 Evidence key 去重，不执行 claim/quote 语义蕴含判断。
- 自动化：BYOK/Evidence 3/3、PDF/BYOK HTTP 2/2、runtime 26/26、npm test 24/24、Playwright 3/3（BYOK 控件 + Mock UI，真实 BYOK 浏览器 E2E 未执行）、contract/typecheck/lint 通过。
- 真实 BYOK 因没有用户凭据保持 `BLOCKED_BY_MISSING_USER_CREDENTIALS`；不把 fake provider 结果写成真实发布通过。

## T-W1-018 V1.0 Release Gate

- T-W1-017 已通过 no-ff merge 进入 main，merge commit 为 `54043fb28cb082eb871773732bafce0f031fc562`。
- Release Gate 自动化复核通过：合并前 main planning 18 tasks；加入 T-W1-018 后当前 planning 19 tasks，runtime 26/26、BYOK/Evidence 13/13、`npm test` 24/24、Playwright 3/3、contract 无 drift、lint/typecheck/build/smoke/security/diff-check 通过。
- PDF fixture SHA-256 保持 `99e07b4ba995b7c90bd84628a5db55b71a4faa1f06d3714a5942741cd39a55f8`；未运行全仓 format。
- 当前环境未发现 `WORKFLOW_BYOK_API_KEY`，因此真实外部 BYOK、真实浏览器 BYOK 和 Evidence 人工抽查未执行；V1.0 保持 `BLOCKED_BY_MISSING_USER_CREDENTIALS`，不创建 tag。

## T-W1-018 Worker polling repair

- 人工 Gate 发现正式 `npm run worker` 只输出 ready 后退出，根因是入口没有调用 `JobRuntime.runOnce()` 或持久轮询；该问题属于 V1.0 发布阻断。
- 新增可测试 Worker loop/service：顺序消费 Job、无 Job 默认 idle 250ms、stop 后不领取新 Job、等待当前 Job 完成、数据库只 close 一次、fatal error 安全摘要、smoke 立即退出。
- 定向 Worker loop 测试 9/9 通过，包含真实 SQLite Job 入队、正式 `JobRuntime.runOnce()` 消费并成功完成；构建后 Worker CLI ready 后保持运行，smoke 子进程正常退出。当前非交互 runner 无法注入真实键盘 Ctrl+C，等价 graceful-stop 已覆盖，需用户终端补做 Ctrl+C 确认。
- 修复不改变真实 BYOK 验收状态：仍为 `BLOCKED_BY_MISSING_USER_CREDENTIALS`，不得标记 `READY_FOR_V1_0_TAG`。
